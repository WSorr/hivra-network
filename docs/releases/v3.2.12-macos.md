# Hivra v3.2.12 for macOS

## Download

Recommended package:

- `Hivra-v3.2.12-macos.dmg`

Alternative package:

- `hivra_app-v3.2.12-macos-release.zip`

## Install

1. Download `Hivra-v3.2.12-macos.dmg`.
2. Open the `.dmg` file.
3. Drag `Hivra.app` into `Applications`.
4. Open `Applications` and launch `Hivra`.

## If macOS blocks the app

This build is currently not Apple signed or notarized, so macOS may block it.

Try this first:

1. Open `Applications`.
2. Right-click `Hivra.app`.
3. Select `Open`.
4. Click `Open` again in the confirmation dialog.

If macOS still blocks it:

1. Try to open `Hivra.app` once.
2. Open `System Settings -> Privacy & Security`.
3. Find the blocked app warning near the bottom.
4. Click `Open Anyway`.

## If macOS says the app is damaged

This can be a Gatekeeper quarantine issue, not actual file corruption.

Run this in Terminal:

```bash
xattr -dr com.apple.quarantine /Applications/Hivra.app
```

Then try opening the app again.

## SHA-256 Checksums

```text
0000f0091f4959015fb0320efc297efec3a6ac44a2aba830eb179a28f958b101  Hivra-v3.2.12-macos.dmg
850a3ab799482ebba31db1ba4998fccbdde0a308d780df68c1b61a7b89b245ef  hivra_app-v3.2.12-macos-release.zip
```
